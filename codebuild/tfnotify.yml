ci: codebuild
notifier:
  github:
    token: $GITHUB_TOKEN
    owner: "Takao-Yamasaki"
    repository: "aws-ecs-development"
terraform:
  plan:
    template: |
      {{ .Title }} <sup>[CI link]( {{ .Link }} )</sup>
      {{ .Message }}
      {{if .Result}}
      <pre><code>{{ .Result }}
      </pre></code>
      {{end}}
      <details><summary>Datails (Click me)</summary>

      <pre><code>{{ .Body }}
      </pre></code></datails>
  apply:
    template: |
      {{ .Title }} <sup>[CI link]( {{ .Link }} )</sup>
      {{ .Message }}
      {{if .Result}}
      <pre><code>{{ .Result }}
      </pre></code>
      {{end}}
      <details><summary>Datails (Click me)</summary>

      <pre><code>{{ .Body }}
      </pre></code></datails>
